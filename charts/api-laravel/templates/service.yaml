apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderonline.fullname" . }}
  labels:
    {{- include "orderonline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orderonline.selectorLabels" . | nindent 4 }}
